sql 문 작성 요령

select

from

where

order by

먼저 기본 형식을 적은 후

들어가야하는 것을 맞춰 넣으면 된다.

case 안에 컬럼이 들어가는 것 비교 중요
case 안에 컬럼이 있으면 swtich 문처럼 사용됨.
case 안에 컴럼며이 없으면 then 안에 컴럼명 안에 값을 비교한 것이 들어간다.

where 절 안에는 true, false 로 나와야 한다.

별칭에 ""을 사용하면 컬럼명으로 인식하지 못한다.