객체는 동작과 데이터를 가지고 있다.

개체는? 데이터만 있는 것을 찾는다. 데이터만? 8가지 자료형으로 분류할 수 있는 것?

사각형으로 엔티티(개체를 표시한다). 주변에 원과 연결한다. 원 안에 속성을 적는다. 기본키에는 밑줄을 긋는다. 


최상위 객체와의 관계에 따라 독립 객체가 될지 않될 지 결정 된다.

개체는 명사 필요한 정보는 다 넣는다. 언제 필요할 지 모르기에 속성 정보를 최대한 찾아 많이 넣는다.

관계를 찾아야 한다. 가장 어려운 것이 관계를 찾는 것이다. 

추가되는 정보만 넣는다.

다대다일 때에만 부산물에 속성을 적는다.

EXERD

staruml

오라클 포트번호 1521

사용자명 입력: system
비밀번호 입력: 1234(커서가 나오지 않는다)

다음에 접속됨:
Oracle Database 18c Express Edition Release 18.0.0.0.0 - Production
Version 18.4.0.0.0

SQL> select *
  2  from tab;



mysql
3306

33060

root
1234


Index of /mysql/Downloads/MySQL-Notifier/

mysql-notifier-1.1.8.msi
